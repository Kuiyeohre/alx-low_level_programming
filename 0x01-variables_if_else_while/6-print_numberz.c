#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * return: always 0 (success)
 */

int main(void)
{
	int k;
for (k = '0'; k <= '9'; k++)
		putchar(k);
	putchar('\n');
	return (0);
}
